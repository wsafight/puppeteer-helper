---
'pptr-helper': minor
---

Add managed Chrome for Testing, remote browser connections, bounded concurrency and batches, retry and recovery policies, network controls and security budgets, result metadata and lifecycle events, element screenshots, deterministic rendering presets, a JSON CLI, an AI rendering skill, structured errors, and production release automation.
