# pptr-helper

## 0.1.0

### Minor Changes

- ee5f9f0: Add managed Chrome for Testing, remote browser connections, bounded concurrency and batches, retry and recovery policies, network controls and security budgets, result metadata and lifecycle events, element screenshots, deterministic rendering presets, a JSON CLI, an AI rendering skill, structured errors, and production release automation.

  Harden response-byte enforcement for streaming and compressed content, clean up BrowserContexts created after cancellation, gate releases on browser tests, and add JSON CLI integration coverage.

  Add CLI JSON Schema validation and NDJSON streaming, browser storage-state portability, diagnostics artifacts, PNG visual regression, composed waits, device and output presets, browser pooling and recycling, priority and tenant/host scheduling, explicit result caching, and structured logging, Prometheus, and OpenTelemetry adapters.
